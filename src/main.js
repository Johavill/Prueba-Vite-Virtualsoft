import { createApp } from 'vue';
import App from './App.vue';
import './style.css';
import 'flowbite'; // Esto carga los JS si los necesitas (como dropdowns, modals, etc.)

createApp(App).mount('#app');
